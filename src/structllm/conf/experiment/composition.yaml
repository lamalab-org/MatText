# @package _global_

defaults:
  - override /model: pretrain_template

model:
  representation: composition
  pretrain.context_length: 32
  pretrain.training_arguments.per_device_train_batch_size: 256
  
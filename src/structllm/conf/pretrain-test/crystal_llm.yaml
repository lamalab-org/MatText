# @package _global_
model:
  logging:
    wandb_project: pretrain-test

  representation: crystal_llm_rep
  pretrain:
    name: pt_30k
    context_length:  512
    training_arguments:
      per_device_train_batch_size: 32
    path:
      data_root_path: /work/so87pot/material_db/structllm_dataset/30k  #--> Change this to folder continaing 30k dataset train.json and test.json
      
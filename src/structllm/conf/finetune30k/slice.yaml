# @package _global_

defaults:
  - override /model: finetune_template

model:
  representation: slice
  finetune.context_length: 512
  finetune.training_arguments.per_device_train_batch_size: 32
  finetune.path.pretrained_checkpoint: /work/so87pot/structllm/results/checkpoints/checkpoints/slice_pretrain_slurm_30k/checkpoint-9000
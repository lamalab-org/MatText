# @package _global_

defaults:
  - override /model: finetune_template

model:
  representation: composition
  finetune.context_length: 32
  finetune.training_arguments.per_device_train_batch_size: 512
  finetune.path.pretrained_checkpoint: /work/so87pot/structllm/results/checkpoints/checkpoints/composition_pretrain_slurm_30k/checkpoint-1000
  
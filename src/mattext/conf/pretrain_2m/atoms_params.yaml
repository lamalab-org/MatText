# @package _global_
model:
  logging:
    wandb_project: pt_2m

  representation: atoms_params
  pretrain:
    name: pt_2m
    context_length: 32
    training_arguments:
      per_device_train_batch_size: 1024
    path:
      data_root_path: /work/so87pot/material_db/mattext_dataset/2m/newreps

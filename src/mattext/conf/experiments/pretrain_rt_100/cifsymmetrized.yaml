# @package _global_
model:
  logging:
    wandb_project: pt_100k_rt

  representation: cif_symmetrized
  pretrain:
    name: pt_100k_rt
    context_length: 1024
    training_arguments:
      per_device_train_batch_size: 64
    path:
      data_root_path: /work/so87pot/material_db/mattext_dataset/100k

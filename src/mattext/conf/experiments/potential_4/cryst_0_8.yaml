# @package _global_
model:
  representation: slice
  alpha: 0.8
  logging:
    wandb_project: potential_lj_slice
  special_num_token: False

  finetune:
    model_name: slice_potential_0_8
    context_length: 512
    training_arguments:
      per_device_train_batch_size: 128
    path:
      pretrained_checkpoint: /home/so87pot/n0w0f/mattext_ckpt/santiago_ckpt_normal/slice_pt_2m/checkpoint-393000

# @package _global_
model:
  representation: slices
  dataset: "bandgap"
  dataset_type: filtered
  special_num_token: False
  checkpoint: n0w0f/MatText-slices-2m
  logging:
    wandb_project: revision-bg-a100

  finetune:
    model_name: revision-bg-a100
    context_length: 512
    training_arguments:
      per_device_train_batch_size: 512
    path:
      pretrained_checkpoint: n0w0f/MatText-slices-2m
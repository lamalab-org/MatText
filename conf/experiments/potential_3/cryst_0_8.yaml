# @package _global_
model:
  representation: composition
  alpha: 0.8
  logging:
    wandb_project: potential_lj_composition
  special_num_token: False

  finetune:
    model_name: composition_potential_0_8
    context_length: 32
    training_arguments:
      per_device_train_batch_size: 1024
    path:
      pretrained_checkpoint: /home/so87pot/n0w0f/mattext_ckpt/santiago_ckpt_normal/composition_pt_2m/checkpoint-12000

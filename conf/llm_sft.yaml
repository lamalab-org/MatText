  hydra:
    job:
      name: llama_sft
    run:
      dir: ${hydra:runtime.cwd}/outputs/${now:%Y-%m-%d}/${now:%H-%M}
    sweep:
      dir: multirun/${now:%Y-%m-%d}/${now:%H-%M}
      subdir: ${hydra.job.override_dirname}


  defaults:
  - model: null



  runs:
  - name: llama_sft_run
    tasks: [llama_sft]


